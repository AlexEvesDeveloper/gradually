<div class="side-user">
    <div class="avatar">
        {% if app.user.image is not null %}
        <img src="{{asset(app.user.image.fullPath)}}" alt="Avatar" width="50px" height="50px" />
        {% endif %}
    </div>
    <div class="info">
        Some stats
    </div>
</div>
<ul class="cl-vnavigation">
    <li>
        <a href="{{path('gradually_home_default_index')}}">
            <i class="fa fa-home"></i>
            <span>Dashboard</span>
        </a>
    </li>
    <li>
        <a href="{{path('gradually_search_job_index')}}">
            <i class="fa fa-search"></i>
            <span>Search for jobs</span>
        </a>
    </li>
</ul>